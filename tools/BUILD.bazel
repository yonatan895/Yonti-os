load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "qemu_runner",
    srcs = ["qemu_runner.rs"],
    edition = "2021",
    deps = [
        "@crates_runner//:bootloader-0.11.15",
    ],
)
